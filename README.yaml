project:
  name: Huge Commit
  description: the application designed to generate commit messages using OpenAI's GPT models.
  license: MIT
  contributions: welcome

dependencies:
  - rust
  - git repository
  - openai api key

features:
  - analyze git diffs
  - generate commit messages
  - interactive confirmation prompts

requirements:
  - rust environment
  - git repository
  - openai api key

installation:
  command: cargo install huge-commit

usage:
  body: execute command bellow
  command: huge-commit help

